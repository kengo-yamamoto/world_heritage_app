<%
=begin%>
 <%= attachment_image_tag @item, :item_image, :fill, 100, 100, fallback: "no_image.jpg" %> 
<%
=end%>
<%= @item.item_name %>
<%= @item.genre.genre_name %>
<%= @item.area.area_name %>/<%= @item.country.country_name %>
<%= link_to "レビュー　採点をする", new_item_review_path(@item.id)  %>
